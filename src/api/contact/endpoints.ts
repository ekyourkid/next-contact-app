import { SERVICE_P_CONTACT } from "./common.service";

export const ENDPOINTS = {
    contact: {
        list: `${SERVICE_P_CONTACT}`,
        create: `${SERVICE_P_CONTACT}`,
        update: `${SERVICE_P_CONTACT}`,
        delete: `${SERVICE_P_CONTACT}`,
    },
};
